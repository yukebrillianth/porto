/**
 * Layout Components - site chrome. See docs/design-brief.md.
 *
 * Navbar and Footer are composed by each page rather than hardcoded into the
 * root layout, so a page can render them inside its own dark section.
 */

export { Footer } from './Footer';
export { Navbar } from './Navbar';
